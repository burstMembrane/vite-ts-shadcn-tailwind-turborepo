#!/usr/bin/env sh

# Format staged files with Prettier
pnpm exec lint-staged --output-logs=none

# Lint workspace (via Turbo pipeline)
pnpm lint --output-logs=none

# Type-check workspace (via Turbo pipeline)
pnpm type-check --output-logs=none
